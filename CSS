.calculator {
    padding: 200px;
    width: 200px;
    margin: 0 auto;
}

input {
    width: 100%;
    margin-bottom: 10px;
    text-align: right;
    padding: 5px;
    box-sizing: border-box;
}

.buttons {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 5px;
}

button {
    background-color: #f2f2f2;
    border: none;
    padding: 15px;
    font-size: 18px;
    cursor: pointer;
}

button:hover {
    background-color: #ddd;
}
